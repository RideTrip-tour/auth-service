# Сервис Аутентификации (Auth Service)

Этот проект представляет собой сервис аутентификации и авторизации, построенный на FastAPI. Он использует библиотеку [FastAPI Users](https://fastapi-users.github.io/fastapi-users/) для реализации основных функций управления пользователями и аутентификации, включая поддержку OAuth2.

## ✨ Возможности

*   Регистрация новых пользователей.
*   Аутентификация с использованием JWT токенов (логин/выход).
*   Сброс и восстановление пароля.
*   Поддержка OAuth2 для входа через сторонние сервисы.
*   Верификация по электронной почте.
*   Управление пользователями (CRUD операции).
*   Защищенные эндпоинты для аутентифицированных и суперпользователей.


## 🚀 Локальный запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/RideTrip-tour/auth-service.git
cd auth-service
```

### 2. Установка зависимостей

Рекомендуется использовать виртуальное окружение.

```bash
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта, скопировав `.env.example` (если он есть), и заполните необходимые переменные окружения.

Пример `.env`:
```env
# Настройки базы данных
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=auth_db

# Секретный ключ для JWT. Сгенерируйте надежный ключ!
# openssl rand -hex 32
SECRET_KEY=ваш_секретный_ключ
```

### 4. Применение миграций базы данных

Убедитесь, что у вас запущен экземпляр PostgreSQL.

```bash
alembic upgrade head
```

### 5. Запуск сервера

```bash
uvicorn src.main:app --reload
```

Сервер будет доступен по адресу `http://127.0.0.1:8000`.

## 📚 API Документация

После запуска сервера интерактивная документация API (Swagger UI) будет доступна по адресу:

`http://127.0.0.1:8000/docs`


## Модель пользователя

**📋 User Table:**
- `id` (PK) - уникальный ID
- `email` (UNIQUE) - email пользователя (320 chars)
- `hashed_password` - хэш пароля (1024 chars)
- `is_active` - активен ли аккаунт
- `is_superuser` - права администратора
- `is_verified` - подтвержден ли email

**🔗 OAuthAccount Table:**
- `id` (PK) - уникальный ID
- `user_id` (FK → user.id) - владелец
- `oauth_name` - провайдер (google/github/etc.)
- `access_token` - токен доступа
- `expires_at` - время истечения токена
- `refresh_token` - токен обновления
- `account_id` - ID у провайдера
- `account_email` - email у провайдера

**📊 Relations:**
- `User 1───* OAuthAccount` (один пользователь → много OAuth аккаунтов)
- Каскадное удаление OAuth записей при удалении пользователя

### **Сценарии использования модели User:**

1. **Регистрация нового пользователя:**
   - Создается запись с `is_active=true`, `is_superuser=false`, `is_verified=false`
   - Пароль хэшируется перед сохранением
   - Email должен быть уникальным

2. **Авторизация:**
   - Проверяется `is_active=true`
   - Сверяется хэш пароля
   - OAuth авторизация через связанные `oauth_account`

3. **Верификация email:**
   - После подтверждения email устанавливается `is_verified=true`
   - Без верификации могут быть ограничены некоторые функции

4. **Административные функции:**
   - Только пользователи с `is_superuser=true` имеют доступ к админке
   - Администраторы могут менять статусы других пользователей

5. **OAuth интеграция:**
   - Один пользователь может иметь несколько OAuth аккаунтов
   - При удалении пользователя каскадно удаляются все его OAuth аккаунты

### **Ограничения и валидация:**
- Email: валидный формат, максимум 320 символов, уникальный
- Пароль: минимум 8 символов, хранится в хэшированном виде
- Все булевы поля имеют значения по умолчанию
- Индексы оптимизируют поиск по email и OAuth провайдерам

---

## Регистрация и подтверждение (verify)

Регистрация выполняется в два шага: запрос регистрации и подтверждение по ссылке из письма. Пользователь создаётся в БД только после успешного подтверждения.

### 1. Запрос регистрации — `POST /register`

**Что происходит:**

- Клиент отправляет `email` и `password` (схема создания пользователя).
- Проверяется, что пользователя с таким email ещё нет. Если есть — ответ `400` с кодом `REGISTER_USER_ALREADY_EXISTS`.
- Пароль валидируется (например, минимальная длина). При ошибке — `400` с кодом `REGISTER_INVALID_PASSWORD`.
- Пользователь **не сохраняется в БД**. Формируется словарь данных (email, хэш пароля и т.д.) и вызывается хук `on_before_register`.

**В `on_before_register` (сервис пользователей):**

- В данные добавляется поле `aud` (audience для токена верификации).
- Генерируется JWT с данными пользователя (email, `hashed_password` и др.). Токен живёт **10 минут** (`verification_token_lifetime_seconds`).
- Формируется ссылка вида: `{origin}/{lk_path}?verufy_token={токен}`.
- На указанный email отправляется письмо «Подтверждение регистрации» с этой ссылкой.

Ответ при успехе: **204 No Content** (тело пустое).

### 2. Подтверждение регистрации — `POST /verify`

Пользователь переходит по ссылке из письма. Фронтенд извлекает из URL параметр с токеном и отправляет его на эндпоинт верификации.

**Тело запроса:** `{"token": "<токен из ссылки>"}`.

**Что происходит:**

- Вызывается `UserManager.verify(token)`.
- Токен декодируется с использованием `verification_token_secret` и проверкой audience.
- Из payload извлекаются email и данные пользователя (в т.ч. `hashed_password`). При невалидном или истёкшем токене — `400` с кодом `VERIFY_USER_BAD_TOKEN`.
- Проверяется, что пользователя с таким email ещё нет (иначе — ошибка «пользователь уже существует»).
- В данные добавляется `is_verified=True`.
- Пользователь **создаётся в БД** через `user_db.create(data)`.
- В ответ возвращается созданный пользователь (схема пользователя).

**Типичные ошибки:**

- `VERIFY_USER_BAD_TOKEN` — неверный/просроченный токен или неверный audience.
- `VERIFY_USER_ALREADY_VERIFIED` — пользователь уже верифицирован (в текущей реализации при создании пользователя через verify он сразу создаётся с `is_verified=True`).

### Схема потока

```
Клиент                    Auth Service                    Email
   |                             |                            |
   |  POST /register              |                            |
   |  { email, password }         |                            |
   | ---------------------------->|                            |
   |                             |  on_before_register:       |
   |                             |  - JWT с данными (10 мин)  |
   |                             |  - ссылка с токеном        |
   |                             | --------------------------->|  письмо со ссылкой
   |  204 No Content              |                            |
   | <----------------------------|                            |
   |                             |                            |
   |  (пользователь переходит по ссылке, фронт берёт token)   |
   |                             |                            |
   |  POST /verify                |                            |
   |  { "token": "..." }          |                            |
   | ---------------------------->|                            |
   |                             |  decode JWT, create user    |
   |  user (201/200 + body)       |                            |
   | <----------------------------|                            |
```

Итог: регистрация считается завершённой только после успешного вызова `POST /verify` с токеном из письма; до этого запись пользователя в БД не создаётся.
